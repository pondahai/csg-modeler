# CLAUDE.md

## 專案是什麼

一個 CSG（構造實體幾何）3D 建模工具，用「實體 ＋ 孔洞」的加減法邏輯建模，操作邏輯參考 Tinkercad。

主要用途是快速做外殼、墊片、治具，並且要能在教學現場使用。

## 使用者背景（重要）

專案擁有者不寫程式。溝通時：

- 用中文回覆，講白話，避免術語堆疊
- 說明「這樣做會怎樣」而不是「這段程式碼怎麼寫」
- 需要決策時，給選項和取捨，不要丟一堆技術細節要求他判斷
- 不要建議他自己去改程式碼或跑指令，除非明確教到那一步
- 改完後告訴他要測什麼、預期看到什麼

## 硬性限制

**單一檔案。** 整個軟體就是 `index.html` 一個檔案，three.js 已內嵌其中。

- 不要拆成多個檔案
- 不要引入建置流程（webpack、vite、npm scripts 等）
- 不要加入任何 CDN 或外部相依，必須能完全離線運作
- 不要引入框架（React、Vue 等）

理由：這個工具要能複製到隨身碟，在教室任何一台電腦上雙擊就開。這個特性優先於程式碼的整潔度。

**不要使用 localStorage 以外的瀏覽器儲存。** 目前用 localStorage 做自動暫存，這是刻意的。

## 架構

檔案內的區塊順序（都在最後一個 `<script>` 標籤內）：

1. **CSG 核心** — 自行實作的 BSP 布林運算（`V`、`Vtx`、`Plane`、`Poly`、`Bsp`、`csg()`）
2. **場景資料** — 節點樹、`evalNode()`、歷史記錄
3. **3D 場景** — three.js 初始化、`rebuild()`、把手
4. **滑鼠互動** — 選取、移動、縮放、旋轉
5. **面板** — 物件樹、屬性面板
6. **指令** — 工具列按鈕、檔案選單、存檔、匯入、匯出

### 資料結構

```js
{
  id, kind: 'shape' | 'group' | 'mesh',
  shape: 'box' | 'cylinder' | 'sphere',   // kind 為 shape 時
  tri: [x,y,z, ...],                       // kind 為 mesh 時（匯入的 STL）。
                                           // 已經搬到原點、正規化成 1×1×1
  role: 'solid' | 'hole',                  // 決定在父層是加還是減
  pos: [x,y,z], rot: [x,y,z],              // rot 單位為「度」
  size: [x,y,z],                           // 物件自己的長寬高（旋轉之前）
  post: [x,y,z],                           // 選填，預設 [1,1,1]。轉完之後才套上去的
                                           // 「世界方向拉伸」，只有轉斜的物件會用到
  color, name,                             // name 為選填
  children: []                             // kind 為 group 時
}
```

### 核心設計決策（改動前務必理解）

**組合必須可逆。** 這是整個專案的核心價值。組合只是在樹上多包一層節點，原始的子物件完整保留在 `children` 裡，參數也都在。任何改動都不能破壞這一點——絕對不要把布林運算結果烘焙成 mesh 取代原始節點。

**存檔存的是樹，不是三角形。** JSON 檔可以事後修改參數。STL 和 OBJ 是輸出格式，不是主檔。

**組合的變換要用 `transformPolys()` 直接套在多邊形上，不要繞經幾何體。**
以前是 `polysToGeo → applyMatrix4 → geoToPolys` 繞一圈，那會把多邊形全部切成
三角形，還會在轉回來時丟掉退化的碎片。每包一層組合就累積一次誤差，做出來的
東西會破洞——實際案例：圓盤＋（機身＋機翼的組合）＋孔洞，射線測試有 2.3% 穿
過去出不來，攤平成同一層卻是好的。改成直接搬頂點之後不但修好，面數還少三成。

**多邊形的法向量用 `newellNormal()`（Newell 法），不要拿前三個點做外積。**
多邊形被布林運算切成細長條之後，前三個點常常幾乎共線，外積會算出接近零的
向量、方向整個亂掉，後續運算就把面分錯邊。這是上面那個破洞的另一半原因。

**幾何一律烘焙在世界座標。** `evalNode()` 回傳的多邊形已套用該節點的變換矩陣，所以 mesh 本身的 transform 永遠是單位矩陣。這讓包圍盒計算和把手定位變簡單。修改時要維持這個前提。

**點一下選取不可以觸發重算。** 以前 pointerdown 一律 `pushUndo()` ＋深拷貝
整個節點，pointerup 只要 mode 是 move/moveY/scaleH/rotate 就無條件 `rebuild()`
——所以「點一下選一個東西」也會跑完整的布林運算。小模型看不出來，匯入大模型
之後就是點一下等三十幾秒，使用者回報「選了之後第一次轉視角會卡很久」，其實
卡的是那一下點選。現在 `pushUndo()` 延後到 `beginEdit()`（真的動到參數才呼叫），
`dragEdited` 為假就不 rebuild，`startState` 也改用 `dragCopy()` 只複製
pos/rot/size/post，不再連幾十萬個三角形一起複製。新增拖曳類操作時要沿用：
**先判斷真的有變化，再 `beginEdit()`。**

**重算是「切成一小段」跑的，不是一口氣算完。** `Bsp.buildG/clipPolygonsG/
clipToG`、`csgG`、`evalNodeG` 都是 generator，每處理 `YIELD_EVERY`（6000）個
多邊形就 yield 一次；`rebuild()` 每次只跑 `SLICE`（25ms）就把主執行緒還給
瀏覽器，所以算很久時畫面還會動、可以按取消，也不會再跳「網頁無回應」。
還控制權用 `MessageChannel`（`soon()`），不要換成 `setTimeout(0)`——後者被
瀏覽器強制最少等 4ms，大模型會被拖慢一大截。

**Web Worker 不能用。** file:// 下瀏覽器會擋掉，而這個工具必須能從隨身碟
雙擊直接開。切段是唯一可行的解，不要再提議搬到背景執行緒。

同步版 `build/clipPolygons/clipTo/csg/evalNode` 都還在，只是改成把 generator
一口氣跑完（`drain()`），匯出 STL／OBJ 還是走同步版。實測輸出與同步版逐點
相同，整體成本 0～10%（8100 面：同步 2538ms、切段 2532ms）。

新幾何體先收在 `built` 裡，全部算完才一次換上場景——中途取消或被下一次重算
取代時，畫面維持上一版完整結果，不會看到半成品；取消時記得 `dropJob()` 把
已經算好的幾何體 dispose 掉，否則會漏記憶體。

**沒被動到的東西不要重算（`geoCache`）。** 以前 `rebuild()` 每次都把場上每一個
最上層物件從頭算一遍，所以移動 A 會連沒碰到的 B（可能是很重的組合）一起重算；
移動組合自己時，裡面的加減關係根本沒變，也整包重算。實際回報：巨量面數的組合
一移動就要等幾十秒。

現在每個最上層物件的幾何體都進快取，鑰匙 `geoKey()` 只放會改變形狀的東西：

- **自己的 `pos/rot/size/post` 不放** —— 那只影響最外層那個矩陣。換位置時直接
  把快取的幾何體套一次差值矩陣（`M新 · M舊⁻¹`），380k 三角形約 80ms，取代原本
  的 33 秒。已驗證與整包重算逐點相同（誤差 7.6e-6，是 float32 的儲存精度，
  重算也一樣），而且**一律從當初算出來的那一份出發**，搬十次也不會累積誤差。
- **`role/color/name` 不放** —— 只影響材質。
- **子節點連自己的 `pos/rot/size/post/role` 一起放** —— 那真的會改形狀。
- 匯入的 `tri` 不 stringify（幾十萬個數字，光算鑰匙就要等），用長度＋取樣點；
  只用 id 不夠，換檔案可能出現相同編號卻是不同模型。開檔與載入暫存都會
  `dropGeoCache()`。

快取自己留一份，場景上永遠是它的複製品，這樣誰該 dispose 很清楚。代價是幾何體
記憶體大約多一倍（379k 面約多 27MB）。新增會改變形狀的操作時，記得那個變化
必須反映在 `geoKey()` 裡，否則畫面不會更新。

**拖曳時不重算 CSG。** 移動、縮放、旋轉的過程中只調整 mesh 的 transform 做預覽，放開滑鼠才呼叫 `rebuild()` 重算布林運算。因為 BSP 運算成本高，每格都算會頓。新增拖曳類操作時請沿用這個模式（參考 `liveMove()`、`liveScale()`）。

**縮放分兩條路，看物件轉得正不正。** `size` 是物件自己的長寬高（旋轉之前），
但縮放的倍率是在世界座標上量的。兩者只有在轉角是 90 度整數倍時才對得起來。

- **轉正的**（`isAxisAligned()` 為真）走老路：用 `axisMap(n)` 把世界軸對應回
  自身軸，倍率套進 `size`。尺寸維持精確的 mm，這是常見情況。
- **轉斜的**：倍率記進 `post`，也就是矩陣裡「旋轉之後」才套上去的世界方向拉伸。
  這樣拉哪一邊就真的往那一邊變形，不管轉幾度。

矩陣順序是 `位移 · post · 旋轉 · size`，post 繞物件自己的中心縮放，不會把物件
推走，所以錨點那套算法照樣成立。拖曳預覽（`liveScale` 直接縮 mesh）也因此跟
放開後的結果完全一致。

**已知取捨**：`post` 是黏在世界軸上的，所以先斜拉、再去轉那個物件，形狀會跟著
歪掉。這是「把手不跟著轉、拉哪邊就往哪邊變形」必然的代價，是刻意選的。另一種
設計是讓把手跟著物件轉，被否決了。旋轉時用 `matrixOf(n,false)` 避開 post，
免得把拉伸分解進 rot/size 裡；拆開組合則是把 post 烘進去後刪掉它。

**每種形狀有固定顏色**，定義在 `SHAPE_COLOR`。新增形狀時要一併加入對應顏色。

## 常見任務

**新增一種形狀**：改 `baseGeo()`（回傳邊長為 1 的單位幾何，實際尺寸靠 `size` 縮放）、`SHAPE_COLOR`、`nodeLabel()`，並在工具列加按鈕。

**新增一個把手**：加到 `handles` 陣列或另建 mesh，在 `placeHandles()` 裡定位，在 pointerdown 的把手判定清單裡加入，並在 pointermove 加對應的 mode 分支。

**工具列**：檔案相關的六個動作（存檔、開啟、插入元件、匯入 STL、匯出 STL／OBJ）
收在 `#fileMenu` 這個下拉選單裡，分區標題用白話寫（「放東西進來」而不是「匯入」），
因為使用者是學生。新增檔案類動作請放進這個選單，不要再往工具列加按鈕。

**改變外觀**：CSS 變數在 `:root`，3D 場景的顏色在 three.js 初始化那一段。工作區背景是淡米黃，兩側面板是深色。

## 部署

推到 GitHub 後由 GitHub Pages 自動發布。`index.html` 必須位於 repo 根目錄。

## 匯入 STL（已經做了，但有天花板）

匯入的 STL 是 `kind:'mesh'` 的節點，跟方塊圓柱走同一條路：三角形被搬到原點、
正規化成 1×1×1 存在 `tri` 裡，實際尺寸記在 `size`，所以 `evalNode()` 只要套
`matrixOf(n)` 就跟其他形狀一模一樣。可以當實體也可以當孔洞，可以組合，可逆。

**BSP 已經改成非遞迴。** `Bsp` 的 `build()`、`clipPolygons()`、`clipTo()`、
`invert()`、`allPolygons()`、`clone()` 全部改用自己維護的堆疊迭代，走訪順序
刻意跟原本的遞迴一致（先前面後後面），所以切出來的結果跟以前逐個多邊形完全
相同（已用單元測試比對過 union／subtract／intersect 及三層巢狀組合）。
爆堆疊（Maximum call stack size exceeded）那道牆因此消失。

現在剩下的天花板純粹是時間。實測在方塊上挖一個匯入的球：

| 面數 | 改之前 | 改之後 | 記憶體 |
|---|---|---|---|
| 3600 | 754ms | 0.5 秒 | |
| 8100 | 爆堆疊 | 4.3 秒 | |
| 14400 | 爆堆疊 | 11.7 秒 | |
| 25600 | 爆堆疊 | 28 秒 | 152MB |
| 40000 | 爆堆疊 | 79 秒 | 428MB |
| 57600 | 爆堆疊 | 185 秒 | 458MB |

耗時大約是面數的平方成長，外推**十萬面一次挖洞落在十到二十分鐘**，
記憶體上看 1GB——分頁有可能先被瀏覽器 OOM 砍掉。這是開到十萬面的
已知風險，不是 bug；`STL_HARD` 是「絕對算不完才擋」的定位，不是保證。

**上限開到十萬面是專案擁有者的決定**：不擋「會很慢」，只擋「絕對算不完」，
值不值得等由使用者自己判斷。所以門檻是
`STL_SOFT=5000`（會頓幾秒，問一聲）、
`STL_HEAVY=20000`（會卡好幾分鐘，用更重的字再問一次）、
`STL_HARD=100000`（擋掉）。

因為 JS 是單執行緒，算的時候畫面完全沒反應（不能轉視角、不能按取消），
看起來就像當掉。HEAVY 那段提示一定要講明這件事，不要為了畫面乾淨拿掉。
十萬面的模型 localStorage（約 5MB）一定存不下，`autosave()` 會亮
「暫存已滿，請手動存檔！」——那是預期行為。`GAUGE_MAX` 調成 `{face:400000, mem:5e6, ms:60000}`——這是照實際使用回報訂的：
場上 379k 面、重算 33 秒，算完之後轉視角拖曳一樣順，因為畫圖是顯示卡的事。
所以 face 那根不該早早滿格；真正會痛的是 ms（每次放開滑鼠都要再等一次），
警告字也改成以 ms 為主。mem 維持 5MB 不要動——那是 localStorage 的硬限制，
是三根裡面唯一會造成「資料不見」的。

**重算成本看的是「有多少面參與布林運算」，不是場上總面數。** 實際回報：
87k 面各自獨立擺著時重算只要 478ms，而拿高面數模型去挖洞一次要 33 秒。
所以效能問題要問「這一刀切了多少面」，不要拿總面數嚇人。挖洞完成後
場上 379k 面照樣順，因為那時候只剩顯示卡在畫圖。

**不要做減面（decimation）。** 專案擁有者明確否決了：匯入的東西必須跟原檔
一模一樣，自動簡化會把螺紋、印章圖案這類細節磨掉，而且使用者不會發現。
太細的模型請使用者自己在別的軟體處理過再進來。也不要為了任何理由改回遞迴。

SVG 匯入當平面參考底圖還沒做，那是另一件事。

## 框選

空白處左鍵拖＝框選。判定方式是**螢幕座標上的矩形重疊**，不是世界座標的盒子：
把每個 mesh 的包圍盒八個角投影到螢幕，取外接矩形，跟拖出來的矩形比重疊
（`screenRectOf()`、`applyMarquee()`）。

這等於「拖出來的框沿著目前視角無限延伸出去」——遠近不影響，被擋住也照樣選到。
這是刻意的，跟 Tinkercad 一致。改動時不要「順手」加上深度或遮擋判定。

按鍵分配因此動過：空白處左鍵拖被框選佔走，所以 **右鍵拖＝轉視角、中鍵拖＝平移**。
筆電觸控板常常沒有中鍵，所以 **Alt＋左鍵拖也是平移**，這個備援不要拿掉。

目前只選最上層節點，選不到組合內部的零件（`selected` 也只裝最上層 id）。

## 油表（畫面右下角）

即時顯示三個負載指標：面數、暫存用量、上次重算耗時。綠→黃→紅，超過八成會跳警告字。
`updateGauge()` 在每次 `rebuild()` 結束時被呼叫，門檻定在 `GAUGE_MAX`。
這是為了 STL 匯入先鋪的路——爆炸前要先看得到油針在動。

## 尚未實作

圓角方塊等進階形狀、文字、匯入 SVG、工作平面、尺標、鏡射、陣列複製、表面吸附。

複雜的參數化建模不在目標範圍內——那類需求交給 FreeCAD。功能要有節制，不要往「重做一個 FreeCAD」的方向長。
