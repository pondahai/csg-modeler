# CLAUDE.md

## 專案是什麼

一個 CSG（構造實體幾何）3D 建模工具，用「實體 ＋ 孔洞」的加減法邏輯建模，操作邏輯參考 Tinkercad。

主要用途是快速做外殼、墊片、治具，並且要能在教學現場使用。

## 使用者背景（重要）

專案擁有者不寫程式。溝通時：

- 用中文回覆，講白話，避免術語堆疊
- 說明「這樣做會怎樣」而不是「這段程式碼怎麼寫」
- 需要決策時，給選項和取捨，不要丟一堆技術細節要求他判斷
- 不要建議他自己去改程式碼或跑指令，除非明確教到那一步
- 改完後告訴他要測什麼、預期看到什麼

## 硬性限制

**單一檔案。** 整個軟體就是 `index.html` 一個檔案，three.js 已內嵌其中。

- 不要拆成多個檔案
- 不要引入建置流程（webpack、vite、npm scripts 等）
- 不要加入任何 CDN 或外部相依，必須能完全離線運作
- 不要引入框架（React、Vue 等）

理由：這個工具要能複製到隨身碟，在教室任何一台電腦上雙擊就開。這個特性優先於程式碼的整潔度。

**不要使用 localStorage 以外的瀏覽器儲存。** 目前用 localStorage 做自動暫存，這是刻意的。

## 架構

檔案內的區塊順序（都在最後一個 `<script>` 標籤內）：

1. **CSG 核心** — 自行實作的 BSP 布林運算（`V`、`Vtx`、`Plane`、`Poly`、`Bsp`、`csg()`）
2. **場景資料** — 節點樹、`evalNode()`、歷史記錄
3. **3D 場景** — three.js 初始化、`rebuild()`、把手
4. **滑鼠互動** — 選取、移動、縮放、旋轉
5. **面板** — 物件樹、屬性面板
6. **指令** — 工具列按鈕、檔案選單、存檔、匯入、匯出

### 資料結構

```js
{
  id, kind: 'shape' | 'group' | 'mesh',
  shape: 'box' | 'cylinder' | 'sphere',   // kind 為 shape 時
  tri: [x,y,z, ...],                       // kind 為 mesh 時（匯入的 STL）。
                                           // 已經搬到原點、正規化成 1×1×1
  role: 'solid' | 'hole',                  // 決定在父層是加還是減
  pos: [x,y,z], rot: [x,y,z],              // rot 單位為「度」
  size: [x,y,z],                           // 物件自己的長寬高（旋轉之前）
  post: [x,y,z],                           // 選填，預設 [1,1,1]。轉完之後才套上去的
                                           // 「世界方向拉伸」，只有轉斜的物件會用到
  color, name,                             // name 為選填
  children: []                             // kind 為 group 時
}
```

### 核心設計決策（改動前務必理解）

**組合必須可逆。** 這是整個專案的核心價值。組合只是在樹上多包一層節點，原始的子物件完整保留在 `children` 裡，參數也都在。任何改動都不能破壞這一點——絕對不要把布林運算結果烘焙成 mesh 取代原始節點。

**存檔存的是樹，不是三角形。** JSON 檔可以事後修改參數。STL 和 OBJ 是輸出格式，不是主檔。

**組合的變換要用 `transformPolys()` 直接套在多邊形上，不要繞經幾何體。**
以前是 `polysToGeo → applyMatrix4 → geoToPolys` 繞一圈，那會把多邊形全部切成
三角形，還會在轉回來時丟掉退化的碎片。每包一層組合就累積一次誤差，做出來的
東西會破洞——實際案例：圓盤＋（機身＋機翼的組合）＋孔洞，射線測試有 2.3% 穿
過去出不來，攤平成同一層卻是好的。改成直接搬頂點之後不但修好，面數還少三成。

**多邊形的法向量用 `newellNormal()`（Newell 法），不要拿前三個點做外積。**
多邊形被布林運算切成細長條之後，前三個點常常幾乎共線，外積會算出接近零的
向量、方向整個亂掉，後續運算就把面分錯邊。這是上面那個破洞的另一半原因。

**幾何一律烘焙在世界座標。** `evalNode()` 回傳的多邊形已套用該節點的變換矩陣，所以 mesh 本身的 transform 永遠是單位矩陣。這讓包圍盒計算和把手定位變簡單。修改時要維持這個前提。

**點一下選取不可以觸發重算。** 以前 pointerdown 一律 `pushUndo()` ＋深拷貝
整個節點，pointerup 只要 mode 是 move/moveY/scaleH/rotate 就無條件 `rebuild()`
——所以「點一下選一個東西」也會跑完整的布林運算。小模型看不出來，匯入大模型
之後就是點一下等三十幾秒，使用者回報「選了之後第一次轉視角會卡很久」，其實
卡的是那一下點選。現在 `pushUndo()` 延後到 `beginEdit()`（真的動到參數才呼叫），
`dragEdited` 為假就不 rebuild，`startState` 也改用 `dragCopy()` 只複製
pos/rot/size/post，不再連幾十萬個三角形一起複製。新增拖曳類操作時要沿用：
**先判斷真的有變化，再 `beginEdit()`。**

**重算是「切成一小段」跑的，不是一口氣算完。** `Bsp.buildG/clipPolygonsG/
clipToG`、`csgG`、`evalNodeG` 都是 generator，每處理 `YIELD_EVERY`（6000）個
多邊形就 yield 一次；`rebuild()` 每次只跑 `SLICE`（25ms）就把主執行緒還給
瀏覽器，所以算很久時畫面還會動、可以按取消，也不會再跳「網頁無回應」。
還控制權用 `MessageChannel`（`soon()`），不要換成 `setTimeout(0)`——後者被
瀏覽器強制最少等 4ms，大模型會被拖慢一大截。

**Web Worker 不能用。** file:// 下瀏覽器會擋掉，而這個工具必須能從隨身碟
雙擊直接開。切段是唯一可行的解，不要再提議搬到背景執行緒。

**算好的幾何體不能跨次開啟快取起來，別再想這條路。** 380k 面存起來約 14MB，
`localStorage` 上限只有約 5MB（而且光存那棵樹就已經用掉八成）。IndexedDB 放得下，
但 file:// 下每個本機檔案被當成不同來源，Chrome 會以跨來源擋掉，還受「封鎖第三方
Cookie」設定影響——跟 Web Worker 同一個死因。也不要改成把三角形存進 JSON 存檔，
那違反「存檔存的是樹，不是三角形」。

所以首次載入一定要重算。能做的是**讓東西一個一個出現**（見下）。

同步版 `build/clipPolygons/clipTo/csg/evalNode` 都還在，只是改成把 generator
一口氣跑完（`drain()`），匯出 STL／OBJ 還是走同步版。實測輸出與同步版逐點
相同，整體成本 0～10%（8100 面：同步 2538ms、切段 2532ms）。

**上場方式看畫面上有沒有東西**（`rebuildG` 的 `stream`）：

- **畫面是空的**（剛開啟網頁、剛開檔）：算好一個就掛一個，先算完的先出現。
  總時間一樣，但不必對著空白畫面乾等，先出現的還能先轉來看。物件樹也先畫出來，
  讓人知道在等什麼。這時 `my.built` 要放掉（東西已經在場上了），中途取消就留著
  半場——下一次重算會走換場那條路把它們清乾淨。
- **畫面上已經有東西**（編輯途中）：新幾何體先收在 `built` 裡，全部算完才一次
  換上，免得看到半成品；中途取消或被下一次重算取代時，畫面維持上一版完整結果。
  取消時記得 `dropJob()` 把已經算好的幾何體 dispose 掉，否則會漏記憶體。

**沒被動到的東西不要重算（`geoCache`）。** 以前 `rebuild()` 每次都把場上每一個
最上層物件從頭算一遍，所以移動 A 會連沒碰到的 B（可能是很重的組合）一起重算；
移動組合自己時，裡面的加減關係根本沒變，也整包重算。實際回報：巨量面數的組合
一移動就要等幾十秒。

現在每個最上層物件的幾何體都進快取，鑰匙 `geoKey()` 只放會改變形狀的東西：

- **自己的 `pos/rot/size/post` 不放** —— 那只影響最外層那個矩陣。換位置時直接
  把快取的幾何體套一次差值矩陣（`M新 · M舊⁻¹`），380k 三角形約 80ms，取代原本
  的 33 秒。已驗證與整包重算逐點相同（誤差 7.6e-6，是 float32 的儲存精度，
  重算也一樣），而且**一律從當初算出來的那一份出發**，搬十次也不會累積誤差。
- **`role/color/name` 不放** —— 只影響材質。
- **子節點連自己的 `pos/rot/size/post/role` 一起放** —— 那真的會改形狀。
- 匯入的 `tri` 不 stringify（幾十萬個數字，光算鑰匙就要等），用長度＋取樣點；
  只用 id 不夠，換檔案可能出現相同編號卻是不同模型。開檔與載入暫存都會
  `dropGeoCache()`。

快取自己留一份，場景上永遠是它的複製品，這樣誰該 dispose 很清楚。代價是幾何體
記憶體大約多一倍（379k 面約多 27MB）。新增會改變形狀的操作時，記得那個變化
必須反映在 `geoKey()` 裡，否則畫面不會更新。

**放開滑鼠時不要清掉拖曳的預覽位移。** 重算是分段跑的，中間隔著幾十毫秒；
如果在 `pointerup` 就把 mesh 的 transform 歸零，畫面會先把物件彈回原位、等新
幾何體算好才跳過去，看起來就是「放開時閃一下跳回去」。預覽要留到 `rebuildG`
換上新 mesh 的那一瞬間才消失（新 mesh 本來就是單位矩陣），中間才接得上。
中途取消時留著也是對的：畫面停在使用者放手的位置。

**拖曳時不重算 CSG。** 移動、縮放、旋轉的過程中只調整 mesh 的 transform 做預覽，放開滑鼠才呼叫 `rebuild()` 重算布林運算。因為 BSP 運算成本高，每格都算會頓。新增拖曳類操作時請沿用這個模式（參考 `liveMove()`、`liveScale()`）。

**縮放分兩條路，看物件轉得正不正。** `size` 是物件自己的長寬高（旋轉之前），
但縮放的倍率是在世界座標上量的。兩者只有在轉角是 90 度整數倍時才對得起來。

- **轉正的**（`isAxisAligned()` 為真）走老路：用 `axisMap(n)` 把世界軸對應回
  自身軸，倍率套進 `size`。尺寸維持精確的 mm，這是常見情況。
- **轉斜的**：倍率記進 `post`，也就是矩陣裡「旋轉之後」才套上去的世界方向拉伸。
  這樣拉哪一邊就真的往那一邊變形，不管轉幾度。

矩陣順序是 `位移 · post · 旋轉 · size`，post 繞物件自己的中心縮放，不會把物件
推走，所以錨點那套算法照樣成立。拖曳預覽（`liveScale` 直接縮 mesh）也因此跟
放開後的結果完全一致。

**已知取捨**：`post` 是黏在世界軸上的，所以先斜拉、再去轉那個物件，形狀會跟著
歪掉。這是「把手不跟著轉、拉哪邊就往哪邊變形」必然的代價，是刻意選的。另一種
設計是讓把手跟著物件轉，被否決了。旋轉時用 `matrixOf(n,false)` 避開 post，
免得把拉伸分解進 rot/size 裡。

**拆開組合不可以無條件把 post 烘掉。** 這裡有過一個會讓形狀突變的坑：矩陣是
`位移 · post · 旋轉 · size`，零件「轉斜了又被拉伸過」時那個矩陣帶著**歪斜**，
而 `decompose()` 只拆得出位移／旋轉／尺寸，拆不出歪斜——它會給一個「最接近」
的答案，形狀就跳掉了。實測：轉 45 度、post 拉 1.5 倍的方塊，組合再拆開變成
43.9 度、20×10×30 變成 25.5×10×38.2。使用者是自己撞到才回報的。

現在分兩條路：
- **組合自己只是被搬過**（沒轉、沒縮放、沒斜拉——剛做好的組合就是這樣，
  而且這是絕大多數情況）：零件的 rot/size/**post** 原封不動帶走，只把位移
  加上去。永遠精確。
- **組合自己被轉過或拉過**：還是只能 decompose。但要先算一遍、把算出來的值
  重組成矩陣跟原本比對（`1e-3` 相對誤差），對不起來就**先問過使用者**，
  講明哪幾個零件會變形、以及「先把組合的旋轉和尺寸歸零再拆」可以避開。
  不要默默把形狀改掉。

**每種形狀有固定顏色**，定義在 `SHAPE_COLOR`。新增形狀時要一併加入對應顏色。

## 常見任務

**新增一種形狀**：改 `baseGeo(n)`（回傳**以原點為中心、剛好塞滿 1×1×1 方框**的
單位幾何，實際尺寸靠 `size` 縮放）、`SHAPE_COLOR`、`nodeLabel()`，並加進
`PALETTE` 陣列（右邊的元件選單）。預設尺寸跟 20mm 立方框不一樣的話，加進
`SHAPE_SIZE`。目前七種：方塊、圓柱、圓錐、球、半球、楔形、六角柱。

「塞滿 1×1×1」這個前提不能破——包圍盒計算、把手定位、縮放那一整套都靠它。
新形狀做出來一定要驗三件事：**封閉**（每條邊剛好被用兩次）、**法向量朝外**
（有號體積 > 0，朝內的話布林運算會把它當成無限大的實體）、**包圍盒是
-0.5～0.5**。半球是用 `LatheGeometry` 車出來的，輪廓兩端都收回中軸，底面
才會自動封起來。六角柱是 `CylinderGeometry(...,6)`，所以 size 量的是**對角**
不是對邊。

**圓角方塊不是獨立的形狀，就是「圓角半徑 `r` > 0 的方塊」**（`boxR()`）。曾經做成
獨立的 `shape:'rbox'`，後來併回去——併了才能「已經擺好的方塊隨時改成圓角的」，
不必刪掉重加，而這在實際建模是有感的。舊檔可能還帶著 `'rbox'`，`migrate()` 在
讀檔時就地換掉。

**選單上不要再加一格「圓角方塊」。** 併回去之後，圓角欄位會出現在每一個方塊的
屬性面板上，本來就看得到了；多一格只會讓人問「這兩個方塊差在哪」，而且用那一格
做出來的方塊只要把圓角改掉，物件樹上的名字就自己變成另一個，更混亂。

**圓角方塊會「自己看 size」，是唯一這樣的形狀。** 圓角半徑是真實的毫米，不能跟著
size 一起被拉扁（方塊拉成 40×5×40 時圓角還是要圓的），所以它用真實尺寸做出
幾何體、再先除以 size，後面照樣乘回來。代價是：

- **有圓角時 `size` 和 `r` 一定要進 `geoKey()`**（在 `kindKey()` 裡），否則改了尺寸
  會走「套差值矩陣」那條捷徑，畫面不會更新——那條捷徑的前提是「形狀沒變」。
  **半徑 0 的方塊不放**，所以最常用的一般方塊完全維持原本的快路，沒被拖慢。
  `shapeFaces()` 的快取鑰匙也要分開（12 面 vs 92 面）。
- 幾何體是用 `ExtrudeGeometry` 的圓弧倒角做的，**three.js 的 bevel 是往外長不是
  往內縮**，所以核心方塊要先減掉 2r 再倒角回來（等於拿半徑 r 的球把方塊滾一圈）。
  直接拿完整尺寸的圓角矩形去倒角會變成 w+2r 寬，實際踩過。

之後如果要做圓管、正 N 邊形柱，同樣的路子（節點多帶一個參數、參數進 geoKey、
屬性面板長出輸入框）已經鋪好了。

**新增一個把手**：加到 `handles` 陣列或另建 mesh，在 `placeHandles()` 裡定位，在 pointerdown 的把手判定清單裡加入，並在 pointermove 加對應的 mode 分支。

**元件選單在右邊面板上半（`#palette`）**，不在工具列。它是「一張畫布墊在底下用
`setScissor`／`setViewport` 切成一格一格畫 3D，上面疊一排透明 `<button>` 負責文字、
hover 和點擊」——這樣文字、鍵盤 focus、無障礙都還是瀏覽器原生行為。滑鼠指著哪一格
哪一格就慢慢轉。縮圖會照 `SHAPE_SIZE` 的比例縮，看到什麼點下去就得到什麼。

它多開一個 WebGL 畫布，開不起來就 catch 掉退回純文字按鈕，功能不會壞。
畫布尺寸要**自己記著比對**（`palW`/`palH`），不能拿 `canvas.clientWidth` 判斷——
它的 CSS 是 100%，一開始就等於面板寬度，`setSize()` 永遠不會被呼叫，畫布內部
卻還停在預設的 300×150，畫出來變形又被裁掉。

**工具列**：檔案相關的六個動作（存檔、開啟、插入元件、匯入 STL、匯出 STL／OBJ）
收在 `#fileMenu` 這個下拉選單裡，分區標題用白話寫（「放東西進來」而不是「匯入」），
因為使用者是學生。新增檔案類動作請放進這個選單，不要再往工具列加按鈕。

**收起側邊面板**：3D 畫面左右邊緣各有一個 `.tgl` 把手，切換 `body.noTree`／
`body.noInsp` 把那一欄的寬度收成 0，並呼叫 `resize()` 更新相機比例。
把手要留在畫面裡（不是留在面板上），收起來之後才點得回去。

**工作平面的有色玻璃**：`glass` 是一張 400×400 的半透明藍色平面，壓在格線下面
一點點（y = -0.02，不然會跟格線互相閃爍）。它的用途是讓人看得出「匯出 SVG 會切
在哪裡」，所以不要拿掉，也不要改成吃光的材質（吃光會隨角度忽明忽暗，看起來像
另一個實體）。它沒有加進 `meshes`／`handles`，點不到，不影響選取。

**改變外觀**：CSS 變數在 `:root`，3D 場景的顏色在 three.js 初始化那一段。工作區背景是淡米黃，兩側面板是深色。

## 部署

推到 GitHub 後由 GitHub Pages 自動發布。`index.html` 必須位於 repo 根目錄。

## 匯入 STL（已經做了，但有天花板）

匯入的 STL 是 `kind:'mesh'` 的節點，跟方塊圓柱走同一條路：三角形被搬到原點、
正規化成 1×1×1 存在 `tri` 裡，實際尺寸記在 `size`，所以 `evalNode()` 只要套
`matrixOf(n)` 就跟其他形狀一模一樣。可以當實體也可以當孔洞，可以組合，可逆。

**BSP 已經改成非遞迴。** `Bsp` 的 `build()`、`clipPolygons()`、`clipTo()`、
`invert()`、`allPolygons()`、`clone()` 全部改用自己維護的堆疊迭代，走訪順序
刻意跟原本的遞迴一致（先前面後後面），所以切出來的結果跟以前逐個多邊形完全
相同（已用單元測試比對過 union／subtract／intersect 及三層巢狀組合）。
爆堆疊（Maximum call stack size exceeded）那道牆因此消失。

現在剩下的天花板純粹是時間。實測在方塊上挖一個匯入的球：

| 面數 | 改之前 | 改之後 | 記憶體 |
|---|---|---|---|
| 3600 | 754ms | 0.5 秒 | |
| 8100 | 爆堆疊 | 4.3 秒 | |
| 14400 | 爆堆疊 | 11.7 秒 | |
| 25600 | 爆堆疊 | 28 秒 | 152MB |
| 40000 | 爆堆疊 | 79 秒 | 428MB |
| 57600 | 爆堆疊 | 185 秒 | 458MB |

耗時大約是面數的平方成長，外推**十萬面一次挖洞落在十到二十分鐘**，
記憶體上看 1GB——分頁有可能先被瀏覽器 OOM 砍掉。這是開到十萬面的
已知風險，不是 bug；`STL_HARD` 是「絕對算不完才擋」的定位，不是保證。

**上限開到十萬面是專案擁有者的決定**：不擋「會很慢」，只擋「絕對算不完」，
值不值得等由使用者自己判斷。所以門檻是
`STL_SOFT=5000`（會頓幾秒，問一聲）、
`STL_HEAVY=20000`（會卡好幾分鐘，用更重的字再問一次）、
`STL_HARD=100000`（擋掉）。

因為 JS 是單執行緒，算的時候畫面完全沒反應（不能轉視角、不能按取消），
看起來就像當掉。HEAVY 那段提示一定要講明這件事，不要為了畫面乾淨拿掉。
十萬面的模型 localStorage（約 5MB）一定存不下，`autosave()` 會亮
「暫存已滿，請手動存檔！」——那是預期行為。`GAUGE_MAX` 調成 `{face:400000, mem:5e6, ms:60000}`——這是照實際使用回報訂的：
場上 379k 面、重算 33 秒，算完之後轉視角拖曳一樣順，因為畫圖是顯示卡的事。
所以 face 那根不該早早滿格；真正會痛的是 ms（每次放開滑鼠都要再等一次），
警告字也改成以 ms 為主。mem 維持 5MB 不要動——那是 localStorage 的硬限制，
是三根裡面唯一會造成「資料不見」的。

**重算成本看的是「有多少面參與布林運算」，不是場上總面數。** 實際回報：
87k 面各自獨立擺著時重算只要 478ms，而拿高面數模型去挖洞一次要 33 秒。
所以效能問題要問「這一刀切了多少面」，不要拿總面數嚇人。挖洞完成後
場上 379k 面照樣順，因為那時候只剩顯示卡在畫圖。

**不要做減面（decimation）。** 專案擁有者明確否決了：匯入的東西必須跟原檔
一模一樣，自動簡化會把螺紋、印章圖案這類細節磨掉，而且使用者不會發現。
太細的模型請使用者自己在別的軟體處理過再進來。也不要為了任何理由改回遞迴。

## 匯入 SVG（已經做了）

SVG 是平面圖形，沒有厚度，所以匯入時跳一個小視窗問**厚度**（預設 2mm）和
**曲線精細度**（粗／中／細＝每段約 1.0／0.3／0.1mm）。擠出來的結果是三角形，
直接存成 `kind:'mesh'` 節點，跟匯入的 STL 走完全同一條路——可以當孔洞、可以
組合、可以存檔、可逆，**不需要新的節點型別**。面數的三道門檻沿用 STL 那組。

**厚度不要改成不問或問兩次。** 專案擁有者的決定：SVG 本身沒有厚度資訊，一定得
由我們定，而匯進來之後拉頂部藍點就能改高度。既然為了精細度一定要開視窗，厚度
就順便放在同一個視窗裡，不多一個步驟。

**不要自己寫 SVG 路徑解析器。** 瀏覽器本身就會解析 SVG：把它掛進畫面（藏在
畫面外，**不能用 `display:none`**，那樣量不到長度），用 `getPointAtLength()`
就能把任何路徑取樣成點——圓弧、貝茲、rect/circle/polygon 全部通吃，連元素身上
和祖先群組的 transform 都算好了（用 `根.getScreenCTM().inverse() × 元素的`）。

**多重子路徑要用「前綴路徑的累計長度」切開。** 一個 `<path>` 裡有好幾個 `M`
（字母 O 的內外圈就是）不能整條取樣，那會把不同的圈接成一條線。作法是把 `d`
拆成一段一段，逐段接起來丟進暫時的 path 量 `getTotalLength()`，得到每一段在
整條路徑上的長度區間，再回到**完整路徑**上取樣那個區間。這樣小寫 `m`（相對
座標）也不會算錯，因為始終是同一個 path 元素在算。

**取樣要取每一小段的中點**（`from + lenU*(i+0.5)/n`），不要取端點。正好踩在
子路徑接縫上時瀏覽器會回傳上一段的終點，那個點會落在別的輪廓邊界上，內外圈
就判錯了——實際症狀：字母 O 的內圈變成第二塊實心板，不是洞。

**內外圈用「奇偶包含」判定**，不是用繞行方向：被奇數個圈包住的就是洞，再把每個
洞掛到「包住它、而且面積最小」的外圈上。方向不可靠（各家軟體輸出不一致），
包含關係可靠。判定時取輪廓上三個點投票，免得剛好踩在別人的邊上。

三角化和擠出交給 three.js **核心裡就有的** `Shape`／`ExtrudeGeometry`（含內孔，
Earcut 在核心裡），不需要外掛的 SVGLoader，不破壞單一檔案的限制。

尺寸：讀 SVG 的 `width`/`height` 換算成 mm（mm/cm/in/pt/pc/px，沒單位當
96dpi），配上 viewBox 得到「一個使用者單位＝幾 mm」。都沒寫就讓長邊等於 50mm。

**整張都沒有填色**（雷切線稿常見）時退而求其次，把每一條線都當成封閉外框，
但要先問一聲。完全找不到輪廓時多半是「文字沒有轉成外框」，提示要講明這件事。

## 匯出 SVG（雷射切割，已經做了）

**取「模型與工作平面相交」的那一層剖面，不是從上往下壓扁的投影。** 這是查證
Tinkercad 的行為之後，由專案擁有者拍板要一致的。所以要切哪一層，就把模型上下
移動去碰工作平面——同一個模型移到不同高度，可以分別匯出外框和內部的刻痕。
**不要改成投影輪廓**：投影得把一堆重疊的輪廓做布林聯集，貴又容易破；剖面法只要
拿三角形跟平面求交，完全不用布林運算，畫面上算好的幾何體直接切。

**切到的線一律當「切穿」，畫成紅色**（雷切軟體辨識切穿的慣例色），不分刻痕。
所以輸出只有線、沒有填色，內外圈的繞行方向就不重要，不必費事判斷。

畫面上工作平面鋪了一層半透明的藍色玻璃（`glass`），就是為了讓人看得出剖面會切
在哪裡、物件有沒有穿過去。

**切的高度是 `SLICE_Y`（千分之一毫米），不是正 0。** 物件預設「坐在」工作平面上，
底面剛好跟平面重合，切在正 0 會切到退化的面、什麼都得不到；抬高一點點之後，
坐在平面上的東西就會乖乖給出它的底面輪廓。

線段接成迴圈時端點要**量化成 1e-4 mm 的格子當鑰匙**，同一個頂點從相鄰兩個三角形
算出來會差在浮點誤差的等級，不量化接不起來。接不起來（模型有破洞）就把開放的鏈
原樣輸出，不要默默丟掉。

**`simplifyRing()` 一定要先併點再刪共線，順序不能反。** 切面在轉角會留下一堆
相距萬分之一毫米的碎點，轉角本身相對這種近鄰的偏離也只有萬分之幾，看起來就跟
共線一樣——不先併掉的話連轉角都會被判成可刪，整圈點全部消失（實際踩過：方塊
外框 73 個點一個不留）。也不要拿「上一個留下來的點」當比較基準，那會讓基準跑到
整條邊的另一端，轉角相對長弦的偏離反而變小，同樣會把轉角刪掉。
修好之後方塊外框剩 4 個點、圓孔 48 個點，檔案 1KB。

已驗證是真剖面而不是投影：半徑 20 的球移到不同高度匯出，寬度分別是
40／36.53／26.27／12.46 mm，跟弦長公式 `2√(r²-h²)` 對得上。

## 框選

空白處左鍵拖＝框選。判定方式是**螢幕座標上的矩形重疊**，不是世界座標的盒子：
把每個 mesh 的包圍盒八個角投影到螢幕，取外接矩形，跟拖出來的矩形比重疊
（`screenRectOf()`、`applyMarquee()`）。

這等於「拖出來的框沿著目前視角無限延伸出去」——遠近不影響，被擋住也照樣選到。
這是刻意的，跟 Tinkercad 一致。改動時不要「順手」加上深度或遮擋判定。

按鍵分配因此動過：空白處左鍵拖被框選佔走，所以 **右鍵拖＝轉視角、中鍵拖＝平移**。
筆電觸控板常常沒有中鍵，所以 **Alt＋左鍵拖也是平移**，這個備援不要拿掉。

目前只選最上層節點，選不到組合內部的零件（`selected` 也只裝最上層 id）。

## 油表（物件樹下方）

即時顯示三個負載指標：面數、暫存用量、上次重算耗時。綠→黃→紅，超過八成會跳警告字。
`updateGauge()` 在每次重算結束時被呼叫，門檻定在 `GAUGE_MAX`。
這是為了 STL 匯入先鋪的路——爆炸前要先看得到油針在動。

原本浮在 3D 畫面右下角，後來搬進 `#tree` 面板底部：`#tree` 是 flex 直排，
`#treeBody` 會捲、油表釘在最下面不跟著捲走。搬進深色面板所以配色跟著改
（底色透明、track 用 `--ink`），三根 fill 的綠／黃／紅維持不變。

**物件樹每個節點都標面數**（`faceCount()`／`faceText()`）：形狀用單位幾何的
三角形數（`shapeFaces()` 算過一次就記起來），匯入的用 `tri.length/9`，組合是
子節點總和。標的是「這個東西自己帶進來多少三角形」，也就是重算成本的來源，
不是算完畫出來的面數（那個在油表）。

## 匯出

**匯出不做任何自動組合。** 專案擁有者的決定：沒有組合起來的東西就是各自獨立的，
最上層的實體各出各的，最上層的孔洞**不會**自動去挖別人。想讓孔洞生效，就先把它
跟實體組合起來——組合裡面的加減本來就會算，那是使用者明確指定的。
以前 STL 會把所有最上層實體聯集、再減掉所有最上層孔洞，那是沒被要求的運算。
不要因為「這樣比較合理」而改回去。

多個獨立物件寫進同一個 STL 就是把三角形接在一起，不做布林聯集（切片軟體本來
就會處理重疊的殼）。最上層的孔洞不是實體，不會被匯出。

**匯出範圍看選取**：有選就只匯出選的（可多選），沒選才是全部（`exportRoots()`）。

幾何體走 `geoForRootG()`，跟畫面同一條路（快取命中就直接沿用），所以匯出得到的
就是畫面上那個東西，而且幾乎不用重算——不要再讓匯出自己跑一次 `evalNode`。
整個流程跟重算共用 `startJob()` 分段跑，畫面不會凍住、可以取消。寫檔的迴圈
（STL 的 DataView、OBJ 的字串）每 `WRITE_CHUNK`（40000）個也要 yield 一次，
不然幾十萬面光是寫檔就會卡住。

## 尚未實作

圓管、正 N 邊形柱、文字、可移動的工作平面、尺標、鏡射、陣列複製、表面吸附。

複雜的參數化建模不在目標範圍內——那類需求交給 FreeCAD。功能要有節制，不要往「重做一個 FreeCAD」的方向長。
